abi <abi/4.0>,
include <tunables/global>

profile "clash-lite" "/opt/clash-lite/clash-lite" flags=(unconfined) {
  userns,

  # Site-specific additions and overrides. See local/README for details.
  include if exists <local/clash-lite>
}
