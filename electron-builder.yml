appId: lite.clash.app
productName: Clash Lite
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!aur/*'
  - '!images/*'
  - '!scripts/*'
  - '!extra/*'
  - '!tailwind.config.js'
  - '!postcss.config.js'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
extraResources:
  - from: './extra/'
    to: ''
protocols:
  name: 'Clash Lite URI Scheme'
  schemes:
    - 'clash'
    - 'mihomo'
win:
  icon: build/app-icon.ico
  target:
    - nsis
    - 7z
  artifactName: clash-lite-windows-${version}-${arch}-portable.${ext}
nsis:
  artifactName: clash-lite-windows-${version}-${arch}-setup.${ext}
  uninstallDisplayName: ${productName}
  allowToChangeInstallationDirectory: true
  oneClick: false
  perMachine: true
  createDesktopShortcut: always
  include: build/installer.nsh
linux:
  executableName: clash-lite
  icon: build/app-icon.png
  desktop:
    entry:
      Name: Clash Lite
      GenericName: Proxy Client
      Comment: A GUI client based on Mihomo
      MimeType: 'x-scheme-handler/clash;x-scheme-handler/mihomo'
      Keywords: proxy;clash;mihomo;vpn;
      StartupWMClass: clash-lite
      Icon: clash-lite
  target:
    - deb
    - rpm
  maintainer: clash-lite
  category: Utility
  artifactName: clash-lite-linux-${version}-${arch}.${ext}
deb:
  afterInstall: 'build/linux/postinst'
  afterRemove: 'build/linux/postuninst'
rpm:
  afterInstall: 'build/linux/postinst'
  afterRemove: 'build/linux/postuninst'
  fpm:
    - '--rpm-rpmbuild-define'
    - '_build_id_links none'
npmRebuild: true
publish: []
